#include "main.h"
/**
 * _issupper -> this is a function to check upppercase alphabets
 * @c: an integer parameter
 * Return: something but success
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
